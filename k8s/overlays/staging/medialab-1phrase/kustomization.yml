apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: halexp-medialab-1phrase

bases:
  - ../../../base/

resources:
  - ingress.yml

patchesStrategicMerge:
  - env.yml

commonLabels:
  stage: staging

images:
  - name: scpomedialab/scpo-experts-search-nginx
    newTag: '0.6.0'
  - name: scpomedialab/scpo-experts-search
    newTag: '0.6.0'
