apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: halexp-sciencespo-nokeywords

bases:
  - ../../../base/

resources:
  - ingress.yml

patchesStrategicMerge:
  - env.yml

commonLabels:
  stage: prod

images:
  - name: scpomedialab/scpo-experts-search-nginx
    newTag: '0.6.2'
  - name: scpomedialab/scpo-experts-search
    newTag: '0.6.2'
